# Kafka to FeedbackMap API

## Description
Simple plain Python application that receives data from a Kafka topic,
transforms it into FeedbackMap API compatible format and then uploads it to
`/rest/map_data_points/`.

## Technology

### Main technologies
- kafka-python
